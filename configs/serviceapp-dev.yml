server:
  port: 8081

spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: consulserver
management:
  endpoints:
    web:
      exposure:
        include: '*'


#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8080/eureka/
